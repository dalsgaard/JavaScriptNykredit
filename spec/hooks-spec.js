var assert = require('assert');

describe('something', function () {
  var something;

  before(function () {
    something = 42;
  });

  it('should ...', function () {
    assert.equal(1, 2);
  })

});
